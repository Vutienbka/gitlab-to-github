# newji

# After clone project:

$ cd dougahisho
$ cp .env.sample .env

# After edit .env file:

$ bundle install
$ rails db:create

# Init DB :

$ rails db:migrate

# Import Seed data DB:

$ rails db:seed